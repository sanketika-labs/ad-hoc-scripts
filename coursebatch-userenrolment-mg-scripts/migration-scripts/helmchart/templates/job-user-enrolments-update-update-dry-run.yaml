{{- if .Values.jobs.userEnrolmentsUpdateUpdateDryRun.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: user-enrolments-update-update-dry-run
  namespace: {{ .Release.Namespace }}
spec:
  template:
    metadata:
      labels:
        app: user-enrolments-update-job
    spec:
      restartPolicy: Never
      containers:
        - name: user-enrolments-update-update-dry-run
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["/bin/bash", "-c"]
          args:
            - python user_enrolments_update/process_csv.py update --config config.yaml --output user_enrolments_update/user_enrolments_output.csv --dry_run true
          volumeMounts:
            {{- toYaml .Values.volumeMounts | nindent 12 }}
      volumes:
        {{- toYaml .Values.volumes | nindent 8 }}
{{- end }}
